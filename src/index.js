import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";


// Ensure the App component is rendered into the DOM node with id="root"
ReactDOM.render(<App />, document.getElementById('root'));
